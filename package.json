{
  "scripts": {
    "start": "yarn run clear && yarn run extract && yarn run combine && yarn run gallerize && yarn run playsets && yarn run clean && yarn run thumbnails",
    "combine": "node src/combine.js",
    "extract": "node src/extract.js",
    "gallerize": "node src/gallerize.js",
    "thumbnails": "node src/thumbnails.js",
    "playsets": "node src/playsets.js",
    "clean": "node src/clean.js",
    "format": "prettier --write src",
    "clear": "rimraf dist/*"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "glob": "^7.1.6",
    "jimp": "^0.16.1",
    "lodash": "^4.17.20",
    "ora": "^4.0.3",
    "p-queue": "^6.6.1",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2",
    "yargs": "^16.0.3"
  }
}
